language: julia
sudo: true
os:
    - linux
julia:
    - nightly
addons:
  apt:
    packages:
      - python3-pip
before_install:
    - sudo apt-get update
    - pip install --upgrade visualdl
script:
    - julia -e 'using Pkg; Pkg.build("VisualDL")'
    - julia -e 'using Pkg; Pkg.test("VisualDL")'
notifications:
    email: true